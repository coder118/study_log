
    //정수 내림차순 배치하기(level_1)
    //숫자를 문자열로 만들어서 쪼개서 배열 만들기
    String[] list = String.valueOf(l).split("");

    //배열 역방향으로 정렬하기
    Arrays.sort(array, Collections.reverseOrder());

    //StringBuilder사용
    StringBuilder sb = new StringBuilder();

    sb.append(문자열, 정수..);

    //StringBuilder을 reverse할 수 있고 문자열으로 간단히 바꿈
    sb.reverse().toString();